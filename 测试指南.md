# 数据导入和测试指南

## 🚀 立即执行步骤

### 第一步：导入数据

1. **打开微信开发者工具**
2. **点击「云开发」按钮**
3. **进入云开发控制台**
4. **左侧菜单选择「云函数」**
5. **找到 `import-data` 云函数，点击进入**
6. **点击「测试」标签页**
7. **输入测试参数：**
   ```json
   {
     "action": "import-all"
   }
   ```
8. **点击「执行」按钮**

### 第二步：查看执行结果

执行成功后，你会看到类似这样的输出：

```json
{
  "success": true,
  "message": "所有数据导入成功",
  "customers": {
    "success": true,
    "total": 282,
    "successCount": 282,
    "failCount": 0
  },
  "products": {
    "success": true,
    "total": 180,
    "successCount": 180,
    "failCount": 0
  }
}
```

### 第三步：验证数据

1. **在云开发控制台，左侧选择「数据库」**
2. **查看 `customers` 集合**
   - 应该显示 282 条记录
   - 点击任意一条记录，查看字段是否完整
3. **查看 `products` 集合**
   - 应该显示 180 条记录
   - 点击任意一条记录，确认字段包含：
     - `material_code` - 商品编码
     - `name` - 商品名称
     - `spec` - 规格
     - `pricing_mode` - 计价模式 (case/piece/unit)
     - `unit_piece_qty` - 件装量
     - `price_piece` - 件价
     - `price_unit` - 包价
     - `unit` - 单位 (件/包)

### 第四步：测试商品和客户选择

1. **在微信开发者工具中，打开「调试器」**
2. **切换到「预览」模式**
3. **进入「客户管理」页面**
   - 应该能看到 282 个客户
   - 客户列表应该正常显示
4. **进入「商品管理」页面**
   - 应该能看到 180 个商品
   - 商品列表应该正常显示
5. **进入「新建订单」页面**
   - 点击「选择客户」，应该能看到客户列表
   - 点击「添加商品」，应该能看到商品列表
   - 选择商品后，应该能正确显示价格

## ✅ 验证清单

请逐项确认：

- [ ] 客户数据：282 条
- [ ] 商品数据：180 条
- [ ] 客户字段完整：name, alias, region, phone, contact, shortName, description
- [ ] 商品字段完整：material_code, name, spec, pricing_mode, unit_piece_qty, price_piece, price_unit, unit
- [ ] 商品单位：只有「件」或「包」
- [ ] 商品双价格：件价和包价都存在
- [ ] 客户选择：新建订单时能选择客户
- [ ] 商品选择：新建订单时能选择商品
- [ ] 价格显示：选择商品后能正确显示对应价格

## ⚠️ 常见问题

### 问题 1：客户/商品数量为 0
**解决方法**：重新执行数据导入

### 问题 2：客户/商品显示为空
**解决方法**：检查云数据库权限设置，确保设置为「所有用户可读写」

### 问题 3：新建订单时无法选择客户/商品
**解决方法**：
1. 检查数据库字段是否完整
2. 检查云函数权限校验逻辑
3. 查看 Console 是否有错误信息

### 问题 4：价格显示不正确
**解决方法**：
1. 检查商品的 `pricing_mode` 字段
2. 检查前端价格选择逻辑
3. 确认 `price_piece` 和 `price_unit` 字段值正确

## 📞 需要帮助？

如果遇到问题，请查看：
1. 云开发控制台的「云函数」->「import-data」->「日志」
2. 微信开发者工具的「调试器」->「Console」
3. 将错误信息截图发给我

---

**重要提示**：数据导入只需执行一次，后续除非手动清空数据库，否则数据会一直存在。
