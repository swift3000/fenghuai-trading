# T52 全业务数据对账审计 + 文档代码对齐（2026-08-27，用户指令"所有业务/页面/算账报表/权限流程全检查一遍"）

## 数据对账审计（只读脚本 tests/data-consistency-audit.js，已挂 test-all 第 1 步常设回归）
8 组断言全过：
1. 订单总额 vs 明细行合计（分位精度）——1 单一致 ✓
2. 订单实收 vs payments 已确认合计 ✓
3. payment_status 与欠款一致性 ✓
4. 孤儿收款记录 = 0 ✓
5. 已取消订单无确认收款 ✓
6. 逐客户赊销守恒（应收=已收+未结清，未结清≥0）✓
7. 用户权限快照 vs 权限矩阵一致 + member:manage 无越权 ✓
8. 引用完整性（订单→客户/商品）+ 无 0 元订单 + material_code 无重复 ✓
- 生产数据面：订单 1 | 收款 0 | 用户 2 | 客户 282 | 商品 167 | 区域 12
- 状态：已做（脚本固化入库，防回归）

## P2-1 API 文档与代码字段不一致（已修）
- 商品段 3.1/3.3：文档写 sku/category/usage/unit_price_piece，代码与生产数据全为 material_code/无 category/无 usage/price_piece+price_unit → 按代码事实重写
- 订单段 6.1/6.2/6.3：文档写 snake_case（order_no/network_time/pagination）+ 不存在的 tmp_ 临时价/usage 自动累计/items_snapshot，生产实为 camelCase（orderNo/customerName）+ 前端 lastOrder 上次价 + items 快照 + 全量返回 → 按生产数据实际 KEYS 重写
- 客户段 4.1/4.3：region_id 外键不存在，实为 region 名称字符串 → 重写
- 区域段 5.1：regions 为 customers 云函数内部 action，非独立 regions 云函数 → 更正 + 现状说明
- 状态：已做

## P3-1 regions 集合 data.data 嵌套脏数据（已清除）
- 12 条记录全部 {data:{name,sort,status}} 嵌套（历史 node-sdk set 坑），另有一条 {status:1} 孤儿
- 业务影响：0——前端不消费 regions 集合（区域下拉用静态名单），regions 云函数无调用方，customers.region 直接存名称
- 处置（2026-08-27 用户拍板清除）：删除前全仓 rg 复核无业务链路读取 → 全量备份 12 条入库（docs/reports/backups/regions-删除前备份-20260827.json）→ 幂等清洗脚本 scripts/cleanup-regions-20260827.js 执行 → 删除 12 条，剩余 0 条，幂等重跑验证通过 → 数据对账审计 12 项全绿
- 状态：已做（若将来启用区域外键链路，从备份 JSON 恢复或重新初始化）

## P3-2 商品「调货」项 price_unit=0（业务设计，非 bug）
- material_code=93 的调货品定价由下单时人工填，unit 模式 0 价占位属预期（is_adjustable=true）
- 状态：关闭（设计确认）

## 销项记录
- 审计脚本固化：tests/data-consistency-audit.js（只读零写入），test-all.sh 第 1 步前置执行
- 文档修正 14 处（3.1/3.2/3.3/4.1/4.3/5.1/6.1/6.2/6.3）
