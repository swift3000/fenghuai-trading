# T51 全量截断清零 + 订单金额服务端重算（2026-08-27，用户指令"其他有 bug 的全修"）

## P1-1 订单总额信任前端（create/update）★动钱核心
- 现象：create/update 直接 `totalAmount = Number(event.totalAmount)` 入库，明细行金额虽是服务端重算，但订单总额=前端传值；API 文档声称"服务端重算，不信任前端"，编码红线"金额服务端算"——文档与实现矛盾
- 后果：前端算错或被篡改 → 应收/收款校验上限/报表/导出全部跟着错
- 修复：create/update 的 totalAmount 改为明细行金额求和后分位取整（itemsSum），前端值仅保留 0 元快速拦截
- 验收：node --check + E2E 回归（下单金额断言）
- 状态：已做

## P1-2 全仓截断清零（第 100 条静默丢失）
- 生产实证：商品 167、客户 282——智能匹配（smart match/parse/parseWithAI 全量拉候选）此刻就在漏匹配第 100 条之后的商品/客户
- 修复清单（fetchAll 分页拉全量，批次 100 防 1MB 上限）：
  - orders：list(200截断) / outboundList(100×2) / runAutoConfirm(500×2) / todayStats(裸get) / 批量 confirmSort/confirmOut(裸get) / order-detail 客户欠款(裸get) / printOrder 客户欠款(裸get) / exportOutbound(裸get)
  - outbound：pendingSortList / pendingOutList / 已出库列表（3 处裸 get）
  - smart：match / parse / parseWithAI（3 处候选全量）
  - system：getAdminWhitelist 用户全量（姓名匹配防漏）
  - report：trend 裸 get + 金额分位累加
  - todayStats/trend 金额改分位整数累加防浮点误差
- 合法保留：doc().get / limit(1) / pendingConfirm limit(50)（明确列表语义）/ 幂等查重 limit(1) / auto_confirm_log 清理循环
- 验收：全仓 rg 复查无裸聚合 get；回归 11 步全绿
- 状态：已做

## 决策记录
- T50-8（order:delete 矩阵收紧）：用户拍板保留四角色删除权（"平常不会删除订单"），T50-3 已确认收款禁删兜底 → 关闭，不收紧

## 销项记录
- T51-1/2 已做：6 个云函数（orders/outbound/smart/system/report + receivable 上轮已修），diff 全量自审 + 残留 rg 复查
- 回归：test:all 11 步（含部署 6 个改动函数）
