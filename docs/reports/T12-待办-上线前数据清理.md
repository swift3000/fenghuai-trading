# T12 上线前数据清理任务卡（2026-08-19 数据审计产出）

> 状态：待办（上线前执行）。**现在不删**，开发/测试期还要用。
> 触发条件：正式提审 / 上线前的上线前检查单环节。
> 执行原则：先备份 → 按特征匹配（非裸 _id）→ 逐项回读 → 删前再报清单给用户确认。

## 背景（2026-08-19 审计）
当前环境数据为开发/测试期数据。其中 **客户、商品 = 原型真实业务数据，保留**；其余为自动化测试残留，上线前清除。

## 保留（真实/原型数据，禁删）
- 管理员账号：`测试`（openid 以 oo0s93 开头）
- customers 集合：100 个客户（手机号 139000000xx / 13800000000 号段 = 原型真实数据，**保留**）
- products 集合：100 个商品（原型真实数据，**保留**）
- system_config / perm_configs / regions：系统配置，保留

## 待删（测试残留，上线前清除）
- users：QA_warehouse / QA_sorter / QA_orderer（openid=qa_test_* 假值）、`QA邀请`×10、`x`×2、空名 orderer 共 15 个测试成员（**保留 admin `测试`**）
- orders：3 个 ¥30 测试单（客户 0088、20260818、confirmed）—— 待确认是否删除（见下）
- payments：上述测试单产生的测试收款记录（若有）

## 待用户确认的边界项（执行前必问）
- `测试客户 QA`（customers，名含“测试”，电话 13800000000）：是否算原型数据？倾向删。
- 商品 `anon`、`全量测试商品`：是否测试残留？倾向删，但需确认非原型数据。
- 6 个订单里的演示单（万友超市 ¥824、东一路刀削面 ¥113.16×2，submitted）：是否保留？

## 执行步骤
1. 备份：导出 customers/orders/users/payments 到 docs/reports/_backup_{date}.json（或用 cloud 导出）
2. 按特征删 users 15 个测试成员
3. 按单号删测试订单 + 关联 payments
4. 回读校验：users 仅剩 admin；orders/payments 无测试残留
5. 跑一次核心流程回归确认不受影响

## 验收
- 上线环境 users=1（仅 admin），无 qa_/QA 残留
- 测试订单与测试收款清零
- 客户/商品原型数据完整保留（100/100）
- 核心流程回归通过
