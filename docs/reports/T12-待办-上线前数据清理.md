# T12 上线前数据清理任务卡（2026-08-19 数据审计 + T13 全量复刻后更新）

> 状态：部分完成。**客户/商品脏名+测试残留已清理（2026-08-19，备份见 docs/reports/_backup_20260819/）**；剩余测试成员/测试订单仍待上线前清除。
> 触发条件：正式提审 / 上线前的上线前检查单环节。
> 执行原则：先备份 → 按特征匹配（非裸 _id）→ 逐项回读 → 删前再报清单给用户确认。

## 背景（2026-08-19 T13 后最新状态）
当前环境数据为开发/测试期数据。**客户、商品 = 原型真实业务数据，保留**；其余为自动化测试残留 + 历史导入产生的"带空格脏名"，上线前统一清理。

### T13 已完成（本次）
- 修复 customers/products 云函数 list 分页 bug（limit 100→1000，原数据超 100 条前端看不全）
- 新增 sync-data 幂等补齐云函数；原型 **282 客户 + 167 商品已 100% 全量同步云端**（正式无空格版全部就位，原型缺失=0）

## 保留（真实/原型数据，禁删）
- 管理员账号：`测试`（openid 以 oo0s93 开头）
- customers：282 个原型客户（正式无空格名，保留）
- products：167 个原型商品（正式无空格名，保留）
- system_config / perm_configs / regions：系统配置，保留

## 待删（测试残留 + 脏名，上线前清除）
### 脏名重复（历史导入带空格，与原型正式版是同一批数据，删除不丢信息）
- customers 6 个：`测试客户 QA`、`东新街 5 号`、`南台路 48 号`、`小学对面面包子店`、`黄焖鸡 1 店`、`黄焖鸡 2 店`
  （正式版 `东新街5号`/`南台路48号`/`小学对面包子店`/`黄焖鸡1店`/`黄焖鸡2店` 已存在；`测试客户QA` 纯测试）
- products 69 个：全部带空格的脏名（如 `淮盐 400g`/`400 克晶纯盐`/`A-3 餐盒` 等）+ 纯测试 `anon`、`全量测试商品`
  （正式版均已就位，删除脏名版不影响任何原型数据）

### 测试成员/订单
- users：QA_warehouse / QA_sorter / QA_orderer（openid=qa_test_*）、`QA邀请`×10、`x`×2、空名 orderer 共 15 个测试成员（保留 admin `测试`）
- orders：3 个 ¥30 测试单 + 其余演示单（待确认）
- payments：上述测试单产生的测试收款记录

## 已完成（2026-08-19 本次，用户拍板）
- 删除 customers 6 个（测试客户QA + 5 个带空格脏名），回读 customers=282 ✅
- 删除 products 69 个（anon/全量测试商品 + 67 带空格脏名），回读 products=167 ✅
- 删前已确认 6 订单均不引用被删客户（ref=0）；删除记录备份于 docs/reports/_backup_20260819/
- 校验：原型 282 客户 + 167 商品 100% 复刻，0 缺失 0 多余 PASS

## 执行步骤（剩余：测试成员/订单/收款）
1. 备份：导出 customers/products/orders/users/payments 到 docs/reports/_backup_{date}.json
2. 删 customers 6 个脏名/测试（先确认订单未引用其 _id）
3. 删 products 69 个脏名/测试（先确认订单未引用；当前仅 6 个测试订单，引用极少）
4. 按特征删 users 15 个测试成员
5. 删测试订单 + 关联 payments
6. 回读校验：customers=282、products=167、users=1（仅 admin）、orders/payments 无测试残留
7. 跑核心流程回归确认不受影响

## 验收
- customers 精确 = 282（原型全量，无脏名/测试）
- products 精确 = 167（原型全量，无脏名/测试）
- users=1（仅 admin），orders/payments 无测试残留
- 核心流程回归通过
