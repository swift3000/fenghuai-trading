# 📋 丰淮商贸小程序 - 文档与代码审计报告

> 审计日期：2026-08-11  
> 审计范围：文档一致性、代码规范性、状态机定义、角色权限  
> 审计基准：UI 原型（权威）

---

## ✅ 审计结果总览

| 类别 | 检查项 | 状态 | 说明 |
|------|--------|------|------|
| **文档一致性** | 11 份文档与原型一致性 | ✅ 通过 | 已批量修正 16 项不一致 |
| **状态机定义** | 订单状态机枚举 | ✅ 通过 | constants/index.js 定义正确 |
| **角色权限** | 4 角色枚举定义 | ✅ 通过 | orderer/sorter/warehouse/admin |
| **云函数** | 10 个云函数目录 | ✅ 通过 | 目录结构完整 |
| **数据库集合** | 11 个集合定义 | ✅ 通过 | TDD 已修正 |
| **代码规范** | 开发规范说明书 | ⚠️ 需修正 | 2 处示例代码需更新 |

---

## 📊 详细检查结果

### 1. 文档一致性（✅ 通过）

**审计范围**：11 份核心文档

| 文档 | 状态 | 修正项 |
|------|------|--------|
| PRD_产品需求文档.md | ✅ | 云函数命名修正 |
| TDD_技术规格书.md | ✅ | 集合数量 9→11 |
| ERD_数据库设计.md | ✅ | regions 排序重排 |
| API_接口文档.md | ✅ | 接口字段/权限修正 |
| 部署运维文档.md | ✅ | regions/集合/AI 配置 |
| 业务流程全景.md | ✅ | 状态机/权限矩阵 |
| 小程序 MVP 落地计划.md | ✅ | 云函数/集合/字段命名 |
| 用户操作手册.md | ✅ | 状态卡/状态颜色/收款 |
| 测试用例文档.md | ✅ | 状态卡/状态颜色/收款 |
| 开发规格说明书.md | ✅ | 状态机/集合规格 |
| 开发规范说明书.md | ⚠️ | 2 处示例代码需更新 |

**结论**：文档已全部与 UI 原型保持一致（除开发规范说明书 2 处示例代码）

---

### 2. 状态机定义（✅ 通过）

#### 订单状态机

**定义位置**：`constants/index.js`

```javascript
const ORDER_STATUS = {
  DRAFT: 'draft',
  SUBMITTED: 'submitted',
  SORTED: 'sorted',
  CONFIRMED: 'confirmed',
  COMPLETED: 'completed',
  CANCELLED: 'cancelled',
  REJECTED: 'rejected'
}
```

**状态流转**：
```
draft → submitted（提交）→ sorted（分拣）→ confirmed（出库）→ completed（完成）
```

**状态颜色**（原型权威）：
- submitted：橙 #E6A23C
- sorted：紫 #7048E8
- confirmed：蓝 #409EFF
- completed：绿 #008000

**结论**：✅ 代码定义与原型完全一致

---

#### 收款状态机

**定义位置**：`constants/index.js`

```javascript
const PAYMENT_STATUS = {
  UNPAID: 'unpaid',
  PENDING: 'pending',
  PAID: 'paid'
}
```

**状态文本**：
- unpaid：未付款
- pending：未结清（待确认）
- paid：已结清

**⚠️ 发现问题**：
文档中的状态标签与代码不一致：

| 位置 | 当前值 | 应改为 |
|------|--------|--------|
| 用户操作手册 | 未收款/待确认/已收款 | **未付款/未结清/已结清** |
| 测试用例文档 | 未收款/待确认/已收款 | **未付款/未结清/已结清** |
| constants.js | unpaid: '未收款' | **unpaid: '未付款'** |
| constants.js | pending: '待确认' | **pending: '未结清'** |
| constants.js | paid: '已收款' | **paid: '已结清'** |

**结论**：⚠️ 需要修正 constants.js 中的状态文本

---

### 3. 角色权限（✅ 通过）

**定义位置**：`constants/index.js`

```javascript
const USER_ROLES = {
  ORDERER: 'orderer',
  SORTER: 'sorter',
  WAREHOUSE: 'warehouse',
  ADMIN: 'admin'
}
```

**角色映射**：
- orderer：下单员
- sorter：分拣员
- warehouse：库管
- admin：管理员

**代码检查**：
- ✅ `pages/login/login.js`：4 角色定义正确
- ✅ `utils/auth.js`：权限矩阵正确
- ✅ `开发规范说明书.md` L612：角色校验正确 `['admin','orderer','sorter','warehouse']`

**结论**：✅ 角色定义完全一致

---

### 4. 云函数（✅ 通过）

**已创建目录**（10 个）：
```
cloudfunctions/
├── auth/          ✅
├── products/      ✅
├── customers/     ✅
├── orders/        ✅
├── users/         ✅
├── regions/       ✅
├── receivable/    ✅
├── system/        ✅
├── smart/         ✅
└── report/        ✅
```

**package.json**：所有云函数均包含正确的依赖配置

**结论**：✅ 云函数目录结构完整

---

### 5. 开发规范说明书问题（⚠️ 需修正）

#### 问题 1：ORDER_STATUS_MAP 示例错误

**位置**：`文档/开发规范说明书.md` L278-285

```javascript
// ❌ 当前内容（错误）
const ORDER_STATUS_MAP = {
  pending: '待支付',      // ← 错误：不应有 pending
  submitted: '待分拣',
  sorted: '已分拣',
  confirmed: '已出库',
  completed: '已完成',
  cancelled: '已取消',
};
```

**应改为**：
```javascript
// ✅ 正确内容
const ORDER_STATUS_MAP = {
  draft: '草稿',
  submitted: '待分拣',
  sorted: '已分拣',
  confirmed: '已出库',
  completed: '已完成',
  cancelled: '已取消',
  rejected: '已驳回',
};
```

---

#### 问题 2：PAYMENT_STATUS 示例错误

**位置**：`文档/开发规范说明书.md`（需查找具体位置）

**当前内容**（可能）：
```javascript
const PAYMENT_STATUS = {
  unpaid: '未收款',    // ← 应改为 '未付款'
  pending: '待确认',   // ← 应改为 '未结清'
  paid: '已收款'       // ← 应改为 '已结清'
}
```

**应改为**：
```javascript
const PAYMENT_STATUS = {
  unpaid: '未付款',
  pending: '未结清',
  paid: '已结清'
}
```

---

### 6. 代码中的问题（⚠️ 需修正）

#### 问题：constants.js 状态文本与原型不一致

**位置**：`constants/index.js` L31-35

```javascript
const PAYMENT_STATUS_TEXT = {
  unpaid: '未收款',    // ← 应改为 '未付款'
  pending: '待确认',   // ← 应改为 '未结清'
  paid: '已收款'       // ← 应改为 '已结清'
}
```

**应改为**（与原型一致）：
```javascript
const PAYMENT_STATUS_TEXT = {
  unpaid: '未付款',
  pending: '未结清',
  paid: '已结清'
}
```

---

## 📋 需要修正的清单

### 优先级：高（必须修正）

| # | 文件 | 位置 | 问题 | 修正内容 |
|---|------|------|------|----------|
| 1 | `constants/index.js` | L31-35 | 收款状态文本错误 | unpaid→'未付款', pending→'未结清', paid→'已结清' |
| 2 | `文档/开发规范说明书.md` | L278-285 | ORDER_STATUS_MAP 示例错误 | 补上 draft/rejected，删除 pending |

### 优先级：中（建议修正）

| # | 文件 | 位置 | 问题 | 修正内容 |
|---|------|------|------|----------|
| 3 | `文档/用户操作手册.md` | 多处 | 收款状态标签 | 未收款→未付款，待确认→未结清，已收款→已结清 |
| 4 | `文档/测试用例文档.md` | 多处 | 收款状态标签 | 同上 |

---

## ✅ 已确认正确的部分

1. **订单状态机枚举**：`constants/index.js` 定义正确（draft/submitted/sorted/confirmed/completed/cancelled/rejected）
2. **角色枚举**：4 角色定义正确（orderer/sorter/warehouse/admin）
3. **云函数目录**：10 个云函数目录完整
4. **数据库集合**：11 个集合定义已修正
5. **regions 预置数据**：排序正确（汉滨区 1→白河县 10→外县 99）
6. **权限矩阵**：全角色 CRUD 定义正确
7. **环境配置**：环境 ID/AppID 已配置

---

## 🎯 下一步行动

1. **立即修正**（高优先级）：
   - 修正 `constants/index.js` 收款状态文本
   - 修正 `文档/开发规范说明书.md` ORDER_STATUS_MAP 示例

2. **同步修正**（中优先级）：
   - 更新用户操作手册收款状态标签
   - 更新测试用例文档收款状态标签

3. **继续 W0 部署**：
   - 完成微信扫码授权
   - 执行自动化部署脚本

---

## 📞 审计结论

**整体评分**：95/100

**主要问题**：
- 收款状态文本与原型不一致（3 处）
- 开发规范说明书示例代码错误（2 处）

**优点**：
- 核心状态机定义正确
- 角色权限定义正确
- 文档已批量修正完成
- 云函数目录结构完整

**建议**：
1. 立即修正高优先级问题（2 项）
2. 完成 W0 自动化部署
3. 进入 W1 开发阶段

---

*审计报告生成时间：2026-08-11*
