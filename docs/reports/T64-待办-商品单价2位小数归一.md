# T64 fenghuai-trading 商品单价 2 位小数归一（2026-09-02，来源：T59-R7B-4 / T63-8 待拍板项，用户拍板"修复"=归一）

状态：已闭环（T64 迁移 commit，test-all 15/15 全绿 EXIT=0，2026-09-02）

## 范围
- 45 个商品文档的 price_unit 超 2 位小数（如 1.266667，整箱价÷数量精确值）→ 四舍五入到分（Math.round×100/100）
- 明确不含：双 0 价"调货"商品（业务项，不在本迁移范围）；存量订单 items 价格快照（订单是独立文档，不受商品库改动影响，已验证生产唯一订单 508 元 paid 原样）

## 关键决策
- 动生产商品数据=动未来新单实际收款 → 备份先行（.local/products-backup-pre-T64.json 全量 167 条）
- 迁移脚本入库幂等可重跑：scripts/migrate-price-round2.cjs（写前复读 where({_id}) 口径、回读校验、条数守恒、残留复扫）
- 踩坑：node-sdk doc(_id).get() 对该集合读 undefined（假 skip，首跑 written=0 残留 45）→ 全改 where({_id:id}) 口径，已沉淀全局错误经验库

## 验证
- 干跑 45 文档/45 处（全 price_unit，无负价，price_piece 整数不碰）
- 落库 45 条成功，残留超 2 位=0，条数守恒 167→167 PASS
- 幂等重跑：written=0 残留 0 PASS
- 数据对账审计 12/12 全绿；生产订单快照未变
- test-all 15 步全量回归全绿 EXIT=0（生产安全态）

## 回滚
`node -e` 从 .local/products-backup-pre-T64.json 按 _id 逐条 update 原值（脚本幂等反向），或 git revert 后重新部署无影响（本次无云函数代码改动，仅数据迁移）
