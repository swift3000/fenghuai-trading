# T50 fenghuai 三轮猎杀审计发现处置（2026-08-27 用户指令：全部流程过一遍，角色流程+赊销财务必须正确）

审计范围：receivable 全量源码 / orders 全量源码 / report 汇总段 / 权限链路（perm_configs→user.permissions）/ 前端调用链 / wx-server-sdk 默认 limit 源码核实。

## P1-1 财务汇总查询无分页，超 100 条静默少算（金额守恒被破坏）★最高危
- 证据：tcb-admin-node/@cloudbase/database query.js L66 `param.limit = 100`（服务端不显式 limit 时默认 100，单次硬上限 1000）
- 受影响（全量聚合却默认截 100 条）：
  - receivable/dashboard L193（赊销三栏：应收/已收/未结清、客户聚合、账龄）
  - receivable/customerDetail L298（客户详情汇总）
  - receivable/exportReceivable（经递归 dashboard 同病）
  - report/summary L262(main)/L277(product)/L340(customer) 三个 tab + L397 payments 台账
- 后果：订单数 >100 后，赊销页/报表/导出全部少算，「应收=已收+未结清」表面仍守恒（都少同样多）但绝对值错——财务报表直接失真
- 修复：receivable 与 report 各加 fetchAll 分页工具（1000/批循环到取空），6 处查询全改走 fetchAll
- 验收：全量回归 11 步全绿；fetchAll 批次=100 与 SDK 默认一致防 1MB 超限
- 状态：已做（receivable 4 处 + report 5 处聚合查询 + 2 处 payments in(ids) 关联全部改 fetchAll 分页拉全量；自审发现并修复 2 处 fetchAll 误用（payRes.data→payRes））

## P1-2 confirmPayment 并发重复确认（双提交可能双记）
- 证据：get(status) → 再 update 两步非原子；两并发请求同时读到 pending 都通过守卫，双双写 confirmed
- 概率：低（库管手动确认，单人操作为主），但收款是资金写路径，按红线必须堵
- 修复：update 改条件更新 where({_id, status:'pending'})，仅 status='pending' 才翻 confirmed；返回 updated 计数==0 走幂等返回
- 验收：条件更新 updated=0 → 幂等 reused:true；幂等专项回归通过
- 状态：已做（where({_id, status:pending}).update 条件更新，并发抢先时幂等返回）

## P1-3 删除订单级联删已确认收款（财务轨迹销毁 + 默认全员可删）
- 证据：orders/delete 无收款状态检查，直接 remove 订单 + 级联删 payments（含 status=confirmed）；删除后客户应收凭空减少，收款台账蒸发，无审计
- 权限面：DEFAULT_MATRIX order:delete 四角色全开（含下单员/分拣员）
- 修复（本期）：后端拦截——查 payments 集合存在 status=confirmed 记录即拒绝删除（3002，查询失败保守拦截）；自审加固：部分收款订单 payment_status 仍为 pending，不能只看状态位；级联清理改只删 pending 记录；前端 canDelete 叠加 paymentStatus!==paid && received<=0
- 决策项（T50-8 单独待办，等用户拍板）：order:delete 是否收紧默认仅 admin（改矩阵需存量用户权限重同步，登录时自愈）
- 状态：已做（拦截部分；决策项见 T50-8）

## P2-4 orders/update-status 无状态机校验（前端已不调用，死接口敞口）
- 证据：orders L413 update-status = 任意 status 直接写库；前端 pages/ 无任何调用（状态流转全走 confirmSort/confirmOut）
- 风险：直连调用可把订单改成任意状态（含 cancelled）绕过状态机；权限=order:edit（四角色默认全开）
- 修复：加合法流转白名单（submitted→sorted→confirmed→completed，允许同态幂等）+ 取消仅 admin + 必填原因 + 目标状态枚举校验 + cancelled_at/cancel_reason/cancelled_by 留痕 + appendLog；前端不用=零影响
- 状态：已做

## ~~P2-5 orders checkPermission 未接 QA 身份~~ 误报，已撤销
- 复核：orders/index.js checkPermission L78 实际为 `const openid = __impersonatedOpenid || OPENID`，已接钩子，与 receivable 一致；role-sim 回归 403 拦截在 T49 回归中通过即为实证
- 状态：取消（审计看漏，非缺陷）

## P2-6 API 文档收款链路描述与实际不符
- 文档 §1.2/§6.7/§6.8 写 orders/collect、orders/collect-confirm；实际收款在 receivable/collect + receivable/confirmPayment（orders 无这两个 case）
- 修复：对照表 18/19/25 行 + 6.7/6.8 章节全部按代码事实重写（函数名/参数名/返回结构/幂等语义）；6.7 原文档参数（payload 包裹 + collect_amount 等）与代码完全不符已纠正
- 状态：已做

## 登记不修（P3/设计口径，说明理由）
- orders/list 硬截断 200 条无分页：当前业务量未触顶；真超量需前端分页改造（独立任务，先观察；登记于此）
- confirmOut ship_* 参数 typeof number 否则 0：前端已验证传 number（outbound.js L263 直接传 large）；保持现状
- 0 值导出留空（exportOutbound 等）：T42 已登记为界面口径一致设计
- receivable/collect 金额+折价双占额口径：pending 收款占额 + discount 登记即累加但确认时只认 confirmed 折价——两口径在 customerDetail/dashboard 统一为「已收=实收+已确认折价」，守恒成立；discount 字段双写（total_discount 登记口径）仅用于 collect 时的剩余欠款占额，不影响台账口径，保持现状

## T50-8 决策项（等用户拍板，未动代码）
- order:delete 默认矩阵四角色全开：是否收紧为仅 admin？收紧需改 DEFAULT_MATRIX（动权限矩阵=高风险），且 save-perm 后存量用户权限快照重同步（登录时自愈）；本期先做"已确认收款禁删"资金红线拦截，矩阵收紧等拍板

## 销项记录
- T50-1/2/3/4/6 已做：云函数 receivable/orders/report 三个资金相关函数 + 前端 order-detail + API 文档，diff 全量自审两遍（自审抓到并修复：fetchAll 返回值误用 2 处、T50-3 状态位判断漏洞 1 处）
- 部署与回归：随 test:all 11 步全量回归验证（部署步骤自动上线变更函数）
- 误报撤销：P2-5（orders checkPermission 已接 QA 钩子，审计看漏）
