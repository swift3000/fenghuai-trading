# T49 fenghuai 二轮深度审查发现处置（2026-08-27 二轮审查产出）

来源：2026-08-27 二轮深审（SAST semgrep 12 项 / 错误码口径 / 元数据 / 部署一致性 / 回归重跑）。
总体结论：无 P0/P1；功能、角色、权限、状态机、金额守恒全部核查通过。本卡收 3 个 P2/P3 登记项。

## P2-1 API 文档错误码表与实际不符（APID）
- 现象：代码实际使用 4001(参数缺失/空名) / 4002(金额超欠款) / 4004(订单不存在) / 5002(text 为空) / 5003(暂无商品数据)，均未登记在 docs/api/API_接口文档.md §1.4 错误码表；且 2001 被"参数错误(订单金额=0)"与"无权限"复用（文档定义 2001=无权限、1001=参数错误）
- 前端影响：无（utils/request.js 仅 code===0 二分支，message 直显）
- 做法：§1.4 错误码表补登记 4001/4002/4004/5002/5003 语义；2001 的订单参数用法加注"历史兼容：订单金额/数量校验沿用语义，新接口一律 1001"；不批量改代码（破坏兼容，走兼容纪律）
- 验收：错误码表覆盖全部实际使用 code；文档与代码 grep 比对无遗漏
- 状态：已做（API 文档 §1.4 补登记 4001/4002/4004/5002/5003 + 2001 兼容注；代码不改，兼容纪律）

## P3-1 sync-data 云函数 package.json name 残留
- 现象：cloudfunctions/sync-data/package.json 的 name 字段="import-data"（复制残留，目录与 cloudbaserc 均为 sync-data）
- 做法：name 改 "sync-data"；纯元数据，不重新部署（name 字段不影响函数运行）
- 验收：node -e require package.json name=sync-data
- 状态：已做（name 改 sync-data，node 校验通过；纯元数据不重新部署）

## P3-2 SAST 扫描 12 项发现登记（semgrep --config auto）
- CI workflow L13-14 actions/checkout@v4、setup-node@v4 可移动 tag 告警：@v4 是 GitHub 官方推荐写法（vN 为语义化主版本 tag，非 SHA 钉死但为社区/官方惯例），豁免
- cloudfunctions/orders L627、report L410、scripts 5 处 console.log 字符串拼接（unsafe-formatstring）：均为 ID 类留痕日志，非用户输入直达格式串，P3 豁免
- scripts/init-db-auto.js L74、init-database.js L59 child_process 参数调用：入参为脚本内硬编码命令，非用户可控，P3 豁免
- 验收：本段登记即为处置
- 状态：已做（本段登记即闭环，12 项全部 P3 豁免）

## 销项记录
- P2-1: API 文档 §1.4 错误码表补登记完成（随 T49 commit 入库；回归 11 步全绿验证前端无感）
- P3-1: sync-data package.json name 修正为 sync-data（node 校验通过；纯元数据，线上函数不受影响）
- P3-2: SAST 12 项发现全部 P3 豁免，登记即闭环（理由见各条）
- 二轮全量回归（11 步）2026-08-27 全绿：单测/权限矩阵/页走查/深走查/部署/E2E/会员/幂等/关钩子/QA 残留校验，收尾确认生产安全态

