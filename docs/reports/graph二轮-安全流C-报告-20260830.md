# graph二轮复扫 · 安全流（测试员C）报告

日期：2026-08-30 | 基线：main=0a1d15f (1.7.0) | 生产态 QA_IMPERSONATE=off
方式：semgrep SAST + 纯 node-sdk 动态探测（匿名态）+ 源码静态走查 + tcb CLI 只读部署核实。未占模拟器，未改任何代码/配置/云函数。

## A. SAST 结果（与 8a79fc2 基线对比）
| 扫描 | 规则数×文件 | findings | 与基线对比 |
|---|---|---|---|
| p/nodejs | 36×65 | 0 | 持平 |
| p/security-audit（含 unsafe-formatstring / detect-child-process） | 23×188 | 2 | **持平** |

2 条 = SC-2 已知白名单标注处（scripts/init-database.js:61、scripts/init-db-auto.js:76，硬编码常量无输入通路，P3 保留观察）。
**SC-1（unsafe-formatstring ×8）复扫清零确认 ✓，无新发现。**

## B. 越权矩阵

**动态验证（node-sdk 匿名态=无微信上下文，等效未登录攻击者；共 16 项全绿）：**
| 项 | 调用 | 预期 | 实测 | 判定 |
|---|---|---|---|---|
| A1 | users.list 带 qaAsOpenid | 钩子惰性→401 | 401 | ✓ 生产钩子确认惰性 |
| B1 | receivable.confirmPayment | 拒绝 | 401 | ✓ |
| B2/B3 | collect 负金额 / users.save-perm | 拒绝 | 401/401 | ✓ |
| B4/B10 | orders.update-status / outbound.confirmOut | 拒绝 | 401/401 | ✓ |
| B5/B6 | system.getConfig/updateConfig | 拒绝 | 2001/2001 | ✓ 未写 system_config |
| B7 | orders.autoConfirmTrigger（cron模态） | 三道闸 | skipped:disabled | ✓ 配置闸拦截 |
| B8/B9 | import-data.verify / qaClearAutoConfirmLog | 拒绝 | 无权限/403 | ✓ |
| B11 | smart.match | 拒绝 | 401 | ✓ |
| D1-D4 | orders.list / receivable.dashboard / report.summary / users.list | 数据不泄漏 | 全 401 | ✓ |

**静态验证（生产态无微信上下文=无法注入 orderer/sorter/warehouse 角色身份；且 node-sdk 匿名调用时 checkPermission 在查 users 前就 401，建临时用户也无法走身份链路——故角色级越权走源码走查）：**
| 端点 | 门禁（源码） | 矩阵口径 | 判定 |
|---|---|---|---|
| receivable.confirmPayment | checkPermission('receivable:confirm') | 仅 warehouse/admin | ✓ 一致 |
| users.*（含 save-perm） | checkAdmin 函数级全门禁 | 仅 admin | ✓ 一致 |
| orders.update-status(cancelled) | 二次校验 role==='admin'+必填原因 | 取消仅 admin | ✓ 一致 |
| system.* 全 action | checkAdmin | 仅 admin | ✓ 一致 |
| orders.autoConfirmTrigger | 带身份→sort:task/warehouse:confirm；无身份(cron)→放行+三道闸 | 内部系统调用 | ✓ 设计如此 |
| import-data / sync-data | checkAdmin | 仅 admin | ✓ 一致 |
| check:perms | npm run check:perms | — | ✓ 三处 perm-matrix 一致 |

## C. Bug 清单（新发现）
| 编号 | 级别 | 发现 | 复现/证据 | 根因 |
|---|---|---|---|---|
| SC-3 | **P0** | clear-all-data 云函数**无鉴权且已部署生产**（tcb fn detail 确认 Active），可清空 customers/products/orders/receivable/outbound/members 6 集合 | 生产态任一小程序用户 wx.cloud.callFunction({name:'clear-all-data'}) 即 wipe 全库；前端未引用（仅测试脚本引用），但函数名可被构造 | 历史工具函数遗留部署，入口无任何身份校验 |
| SC-4 | P2 | init-db（无鉴权，可建集合+写测试文档）、check-customer-fields（无鉴权，泄漏 5 条客户样本）同样部署生产 | tcb fn list 确认均 Deployment completed | 同类遗留工具函数 |
| SC-5 | P2 | admin 门禁（users/system/import-data/sync-data 的 checkAdmin）不校验 user.status，被禁用 admin 仍可改 system 配置（AI 密钥等）；业务侧 checkPermission 有 status 校验，口径不一致 | 源码比对：4 处 checkAdmin 均只判 role 不判 status | 门禁实现不统一 |
| SC-6 | P2 | orders.create 不校验行级负单价：负价行（qty>0）可混入正价行使 itemsSum>0 通过，压低订单总额/应收 | 静态：normalization 段无 price<0 拦截，仅 itemsSum<=0 兜底；需身份利用，匿名 401 | 服务端只做了总额兜底，未做行级价格校验（业务红线：金额相关） |
| SC-7 | P3 | receivable.collect 非数字 amount（如 'abc'）通过 `!amount || amount<=0` 守卫（JS 字符串比较为 false），按 0 元登记 pending 收款 | 静态：toCents('abc')=0，0>remaining 不成立→写 0 元 pending 记录 | 金额未先做 Number 强转+NaN 校验 |
| SC-8 | P2 | docs/api/openapi.yaml 与代码漂移：7 个 phantom action（orders.pick-stash/mark-printed/collect-confirm/update-remark、customers.customer-detail、products.product-detail、regions.create/update）代码中不存在（grep 0 命中） | openapi 2026-08-27 T42 建成，代码侧无对应 case | 文档按规划稿写而非按代码生成，之后代码改名/删除未同步 |

**已复核无问题（1.7.0 生效确认）：** auth 日志脱敏（openid 截 6 位+***、inviteCode 打码）✓；全仓云函数无全量 openid 日志 ✓；硬编码密钥扫描 0 命中（仅公开 appid 属正常）✓；.env 从未入库（git log 全历史 0 命中，.gitignore 覆盖）✓；ci/*.key 已 gitignore（仅 ci/upload.js 入库）✓；CI actions pin SHA ✓（checkout d632683…/setup-node 1d0ff46…）。smart.transcribe 的 audioUrl 为客户端可控但仅转交腾讯云 ASR 拉取（非本地 fetch），SSRF 面有限，不立卡。

## D. 临时用户清理确认
**未创建任何临时用户（残留=0，node-sdk where openid^TEST_graph 回读=0）**。原因：生产态 QA 钩子惰性（A1 已实证），node-sdk 匿名调用在查 users 前即 401，临时用户无法构成身份链路——按任务书预案改走"动态匿名矩阵+静态走查"双轨验证。
