# T11 fenghuai 质量债任务卡（2026-08-19 审计产出）

> P1（出库/收款事务）已单独执行，见 commit 记录。以下为 P2。

> **状态（2026-08-19 收尾）**：4 项全部完成，已 push 至 codex/mvp-hardening 分支。
> - P1-fix 幂等预检补全 + P2-3 幂等键：已完成（commit 7e181ba）——collect 复用已有 pending（clientToken 幂等键）、confirmPayment/confirmSort/confirmOut 状态守卫、orders 持久化 last_pending_payment_id、前端两入口打开弹窗时生成 collectClientToken。
> - P2-1 空 catch 吞错：已完成（commit 289fe87）——2 处补 console.error + 可感知错误返回。
> - P2-2 金额口径：已完成（commit 9d809aa）——orders 2 处 + report itemAmount 共 3 处计算点改 Math.round(x*100)/100 分位取整；AGENTS.md 业务红线加金额口径约定（v1.1→v1.2）。
> - 验证：node --check 全过；npm run test 中 4 个 AI 中转站测试失败为 pre-existing（stash 对比确认与本改动无关）；回归依赖微信开发者工具（QA_IMPERSONATE），本次未跑，如实标注。
> - 未闭环：P1-fix 真机验收（快速双击只新增 1 条 pending）——云函数未部署/未真机测，留项目线程执行。
## P1-修正 幂等预检补全（2026-08-19 复审降级结论）
- 复审确认：confirmPayment 幂等重算+pendingCents 预检设计正确，**不加跨集合事务**（见 advantages.md）
- 残留真缺口：collect 返回的 paymentId 未持久化到订单，重试会重复生成 pending 记录（预检能拦超额但不能拦重复）
- 做法：collect 在 orders 文档写 last_pending_payment_id；前端重试先查已有 pending 直接复用
- 验收：同一订单快速双击登记，payments 只新增 1 条 pending

## P2-1 空 catch 吞错（2 处）
- 位置：cloudfunctions/orders/index.js:623、report/index.js:366
- 做法：catch 内至少 console.error + 返回可感知错误/降级值；不静默
- 验收：grep 'catch (e) {}' 0 命中

## P2-2 金额口径统一（存分不存元）
- 现状：toFixed(2) 字符串入展示与部分入库字段
- 做法：DB 存金额改整数分（分）或保持 2 位字符串但明确"字符串金额=元，禁数值运算"；改一个集合写一次迁移+回归
- 验收：orders/receivable 相关回归全绿；金额守恒检查通过

## P2-3 幂等键
- 高危写接口（收款确认/出库确认）加幂等键（唯一约束或 event_id）
- 验收：重复调用不重复入账
