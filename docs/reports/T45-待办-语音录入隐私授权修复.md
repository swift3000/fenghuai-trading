# T45 · 语音录入 fail:start 修复（隐私授权链路）

- **状态**：已做（代码侧）；待用户在公众平台配置「用户隐私保护指引-麦克风」后真机验证
- **现象**：真机点语音录入报 operateRecorder:fail:start record fail
- **根因**：① 公众平台未声明麦克风隐私接口（微信 2023 起录音属隐私接口，未声明则 start 直接失败）② 代码缺新版基础库 requirePrivacyAuthorize 前置检查，报错提示为原始错误码不指引
- **代码改动**：pages/new-order/new-order.js startVoiceInput 增加隐私授权前置 + onError 对隐私/权限类失败给明确指引
- **用户必做（代码救不了）**：mp.weixin.qq.com → 设置 → 服务内容声明 → 用户隐私保护指引 → 申请/补充「麦克风（录制音频）」→ 提交（一般即时生效，个别 24h 内）
- **验证**：node --check 通过；真机路径=新建订单→智能录入→语音录入→按住录音→松手→出文字（开发者工具无麦克风，必须真机）

