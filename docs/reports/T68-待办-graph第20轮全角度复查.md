# T68 — graph 第20轮（R20）全角度复查 + 新边界角度

- **日期**：2026-09-02
- **范围**：四路全角度（R19 已证核心链路/资金/权限/状态机/幂等/token 全绿）+ 本轮新增边界角度（0.01 分钱单/折价越界/编辑守卫/0件0包过滤/报表四tab导出/客户删除引用/update 数量上限）
- **环境**：QA 钩子 10/10 开测→全程→10/10 关测（生产安全态）

## 结论

发现 1 个真 P2（0件0包商品行在订单详情页显示，违反业务红线），已修（T68，commit 878c610）。其余边界角度全绿；main tab 导出空=后端死分支（前端不可达，非 bug）。

## 新边界角度结果

| 角度 | 结果 |
|---|---|
| E1 最小金额 0.01 元单 | 可建 totalAmount=0.01 |
| E2 折价越界（折价999>应收10） | 被拒 |
| E3 编辑状态守卫（sorted 后编辑） | 拒 3002 且未改数量 |
| E4 0件0包商品行过滤 | 已修 T68（create 落库含 0件0包 行→详情页显示，违反"不显示"红线） |
| E5 报表四 tab 导出 | product/customer/payment 过；main=前端不可达死分支（无 main tab），非 bug |
| E6 客户删除引用回执 | 明确回执（非 500） |
| E7 T66 update 数量上限 | 50000 截断 9999 |

## T68 修复（0件0包 行显示）

- 根因：orders create 落库用 normalizedItems（含 0件0包 行），update 落库用 validItems2（已过滤）——两口径不一致。detail 返回不过滤 → 订单详情页 wx:for 直接渲染 0件0包 商品行。打印销售单本已过滤（pq/zq 判断），详情/打印口径不一致。
- 业务红线：已成订单中 0件+0包商品行不显示——详情页违反。
- 修复：create 落库改用 validItems（对齐 update），一处最小差异。totalAmount 本就按 validItems 计算，不影响金额；validItems 必非空（空已被 2001 拦截）。
- 回归（t68verify 6/6）：落库过滤 1 行 / detail 返回 1 行 / 打印正常 / 纯 0件0包 单仍拒 2001 / 混合单金额 25 不变
- 全量回归：test-all 15/15 全绿、0 失败、QA 残留校验通过（生产安全态）

## 收尾

- QA 钩子 10/10 关闭（生产安全态）
- TEST 残留=0，基线恢复 customers 282 / products 167 / orders 1 / payments 1 / users 2
- T68 修复回归 6/6 + test-all 15 步全绿

