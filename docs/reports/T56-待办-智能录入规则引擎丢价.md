# T56 任务卡：智能录入规则引擎丢价（SA-1 复测确认）

状态：已做（commit c293753，2026-08-30；分支 fenghuai/bugfix-t56-smart-price）

来源：graph 测试 A 路报 SA-1（P1，智能录入丢价无法建单）；2026-08-30 用户指令"再跑一遍再测一遍，修复哪个问题就修哪个"后复测定位。

## 根因（复测三层排除法定位）
- 商品原件层：167/167 商品均有 price_piece/material_code（老酸奶 piece=45），排除。
- 金额算法层：orders 服务端重算 T51-2 正确，排除。
- **代码层（根因）**：smart 云函数规则引擎（parseOrderText/parseMultiProductText）返回 item 仅 {_id,name,spec,price,qty}，缺 material_code/pricing_mode/price_unit/unit；前端 parseOnline 用 `if (it.material_code)` 判"已匹配商品"，规则命中项无 material_code → 落"未匹配自由项"分支 price 置 0 → 总额 0 → 建单被"订单金额为 0"拦截。仅 AI 路径（buildItems）字段齐全。规则引擎是主路径（AI 为可选兜底），故普通文字录入必踩。

## 修复方案
smart 云函数新增 normalizeRuleItems(items, products)：按 _id 回查商品库补 material_code/pricing_mode/price_unit/unit（缺省 unit='包'，与 AI 路径 buildItems 同口径）；parse 与 parseWithAI 两处返回前归一化。纯增字段，不破坏 AI 路径（已有字段不覆盖）。

## 防回归
tests/smart-ai-test.js 补断言：规则引擎返回项含 material_code（命中项）。

## 验收
1. 单测绿（含新断言）
2. 部署 smart 后 QA 钩子态 parseWithAI("老酸奶 2件") 返回项 material_code 非空+price=45
3. 全量回归 12 步全绿

## 验收结果（2026-08-30）
- ✅ 单测：smart-ai-test 5/5 全绿（含 T56 新断言：规则命中项带 material_code='001'/unit/price=45/pricing_mode）；smart-dialect-test 16/16 无回归
- ✅ 纯函数层实测：parseOrderText('老酸奶 2件')+normalizeRuleItems → {_id,name,price:45,qty:2,unit:'件',material_code:'7',pricing_mode:'case',price_unit:1.5}；5 种输入格式（老酸奶 2件/鲜爽果汁 3箱/老酸奶2件/2件老酸奶/鲜爽果汁 3件）全部带回 unit+material_code
- ✅ smart 云函数已部署（✅ smart 部署完成）
- ✅ 分支全量回归 12 步全绿：对账 12/12+perm-logic 19/19+权限 UI 12/12+页走查 13/13+deepwalk 24/24+403 矩阵 28/28+状态机守卫 12/12+E2E 23/0+会员 56/0+幂等 10/0+QA 残留门禁✅，生产安全态
- 顺带修复：tests/wx-server-sdk-mock.js 预存缺陷（缺 query.limit/skip/count 链，T51-1 fetchAll 分页后 mock 未跟上，smart-ai 套件 5 用例此前全部挂掉且不在 test:all 内未暴露）
