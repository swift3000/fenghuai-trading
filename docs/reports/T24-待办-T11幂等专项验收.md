# T24 T11 幂等专项验收 + 全量回归收尾（2026-08-21）

> 状态：✅ 已做（commit 9932eb7 / ecd03e7 + 本卡提交）

## 内容
1. 新增 tests/wx-empirical-idem-test.js（幂等专项，挂 test-all.sh 第 9 步，全量 10→11 步）：
   - 造 TEST 订单 → 同 clientToken 双击 collect → 断言复用同一 paymentId、payments 只 1 条
   - 重复 confirmPayment → 断言 received_amount 只累加 1 次、payment_status=paid
   - 走云函数 orders/detail 读数据（node-sdk 直查在本环境读空/分裂，禁用）
2. 云端实测 10/10 全绿；QA 钩子关回生产安全态（10 函数 QA_IMPERSONATE 全空）
3. 完整 11 步全量回归（npm run test:all，2026-08-21）：11/11 全绿，172 断言 0 失败，收尾门禁通过（QA 残留 0），结束态=生产安全态
   - 各步：1 perm-logic 19 | 2 wx-perm-ui 12 | 3 pagewalk | 4 deepwalk 24 | 6 role-sim 28 | 7 e2e-flow 23 | 8 member-mgmt 56 | 9 幂等专项 10 | 10 关钩子 | 11 门禁
4. T11 任务卡销项；AGENTS.md 步骤数同步；调试残留脚本已清除

## 遗留
- 无 P0/P1；唯一遗留=T12 上线前数据清理（测试成员/订单/脏名，用户提审前触发）

## 2026-08-24 重跑复核（全量回归第 9 步闭环）
- 真机调试退出后无头 auto 会话不再抢端口，重跑 10/10 全绿（IDEM_EXIT=0）：同 token 双 collect 只 1 条 payments、双 confirm 只入账 1 次、TEST 数据自清理
- 关钩子后复核：10 函数 QA_IMPERSONATE 全空、残留 0、cloudbaserc 无 diff
- 注：此前 3 次 FATAL 均为「开发者工具 GUI 连真机调试 + 无头 auto 会话抢端口」链路抖动，非代码缺陷；另遇 tcb COS 上传 ETIMEDOUT，重跑即过
