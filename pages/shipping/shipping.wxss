/* 打印页面 */
.shipping-page {
  background: #fff;
  min-height: 100vh;
  padding: 40rpx;
}

/* 打印预览区域 */
.preview-area {
  background: #fff;
  border: 1rpx solid #e5e5e5;
  box-shadow: 0 2rpx 10rpx rgba(0, 0, 0, 0.05);
  padding: 40rpx;
  margin-bottom: 40rpx;
}

/* 标题区域 */
.shipping-header {
  text-align: center;
  margin-bottom: 40rpx;
  padding-bottom: 30rpx;
  border-bottom: 2rpx solid var(--theme-primary);
}

.shipping-header .title {
  font-size: calc(40rpx * var(--font-scale));
  font-weight: bold;
  color: #333;
  display: block;
  margin-bottom: 10rpx;
}

.shipping-header .subtitle {
  font-size: calc(24rpx * var(--font-scale));
  color: #666;
  letter-spacing: 4rpx;
}

/* 信息行 */
.info-section {
  margin-bottom: 30rpx;
}

.info-row {
  font-size: calc(28rpx * var(--font-scale));
  line-height: 2;
  display: flex;
}

.info-row .label {
  color: #666;
  min-width: 120rpx;
}

.info-row .value {
  color: #333;
  font-weight: 500;
}

/* 客户信息区域 */
.customer-section {
  margin-bottom: 30rpx;
  padding: 20rpx 4rpx;
}

.customer-name {
  font-size: calc(30rpx * var(--font-scale));
  font-weight: 600;
  color: #333;
}

/* 商品表格 */
.items-table {
  margin-bottom: 30rpx;
  border: 2rpx solid #333;
}

.table-header,
.table-row {
  display: flex;
  padding: 15rpx 12rpx;
  border-bottom: 1rpx solid #ddd;
  font-size: calc(26rpx * var(--font-scale));
  align-items: center;
}

.table-header {
  font-weight: bold;
  background: #f5f5f5;
  color: #333;
}

.table-row:nth-child(even) {
  background: #fafafa;
}

.table-row:last-child {
  border-bottom: none;
}

/* 列宽（对齐原型销售单：编号/产品/规格型号/单位/总数量/单价/金额/备注） */
.col-idx { flex: 0.6; text-align: center; }
.col-name { flex: 2; }
.col-spec { flex: 1.4; }
.col-unit { flex: 0.7; text-align: center; }
.col-qty { flex: 1; text-align: center; }
.col-price { flex: 1.1; text-align: right; }
.col-amount { flex: 1.2; text-align: right; }
.col-remark { flex: 1.2; }

/* 合计行（表内） */
.total-row.in-table {
  display: flex;
  padding: 15rpx 12rpx;
  border-top: 2rpx solid #333;
  font-weight: bold;
  background: #f9f9f9;
}
.total-row.in-table .col {
  align-items: center;
}

/* 汇总/欠款行 */
.summary-line {
  display: flex;
  justify-content: space-between;
  font-size: calc(26rpx * var(--font-scale));
  padding: 20rpx 4rpx;
  font-weight: 500;
}

.debt-line {
  font-size: calc(28rpx * var(--font-scale));
  font-weight: bold;
  color: #c0392b;
  padding: 10rpx 4rpx 20rpx;
}

/* 页脚信息 */
.footer-info {
  display: flex;
  justify-content: space-between;
  margin-top: 20rpx;
  padding: 20rpx 0;
  border-top: 1rpx solid #e5e5e5;
  font-size: calc(24rpx * var(--font-scale));
  line-height: 2;
}

.footer-right {
  text-align: right;
}

.so-note {
  font-size: calc(22rpx * var(--font-scale));
  color: #888;
  line-height: 1.8;
  margin-top: 15rpx;
  border-top: 1rpx dashed #ddd;
  padding-top: 15rpx;
}

/* 合计区域 */
.total-section {
  margin-top: 30rpx;
  padding: 25rpx;
  background: #f9f9f9;
  border-radius: 8rpx;
}

.total-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 15rpx;
  font-size: calc(28rpx * var(--font-scale));
}

.total-row:last-child {
  margin-bottom: 0;
}

.total-label {
  color: #666;
  font-weight: 500;
}

.total-value {
  font-size: calc(32rpx * var(--font-scale));
  font-weight: bold;
  color: #333;
}

.total-value.text-success {
  color: var(--theme-primary);
}

.total-value.text-danger {
  color: #FF3B30;
}

/* 页脚 */
.footer {
  margin-top: 40rpx;
  padding-top: 30rpx;
  border-top: 1rpx solid #e5e5e5;
}

.footer-text {
  display: block;
  font-size: calc(26rpx * var(--font-scale));
  color: #666;
  line-height: 1.8;
  margin-bottom: 25rpx;
}

.signature-section {
  display: flex;
  justify-content: space-between;
  margin-top: 30rpx;
}

.signature-item {
  font-size: calc(26rpx * var(--font-scale));
  line-height: 2;
  display: flex;
  align-items: center;
}

.signature-label {
  color: #666;
  margin-right: 10rpx;
}

.signature-value {
  color: #333;
  border-bottom: 1rpx solid #333;
  padding: 0 20rpx;
  min-width: 150rpx;
  display: inline-block;
}

/* 操作按钮 */
.action-buttons {
  margin-top: 50rpx;
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.action-buttons .btn {
  width: 100%;
  height: 90rpx;
  line-height: 90rpx;
  border-radius: 12rpx;
  font-size: calc(32rpx * var(--font-scale));
  font-weight: 500;
  border: none;
  display: flex;
  align-items: center;
  justify-content: center;
}

.action-buttons .btn-primary {
  background: linear-gradient(135deg, var(--theme-primary) 0%, var(--theme-primary) 100%);
  color: #fff;
}

.action-buttons .btn-secondary {
  background: linear-gradient(135deg, #FF8A2B 0%, #f57a1a 100%);
  color: #fff;
}

.action-buttons .btn-tertiary {
  background: #f5f5f5;
  color: #666;
}

.action-buttons .btn:active {
  opacity: 0.8;
}

/* 弹窗样式 */
.modal-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  z-index: 999;
}

.modal-content {
  position: fixed;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
  background: #fff;
  border-radius: 16rpx;
  width: 80%;
  max-width: 600rpx;
  z-index: 1000;
  overflow: hidden;
}

.modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 30rpx 35rpx;
  border-bottom: 1rpx solid #e5e5e5;
}

.modal-title {
  font-size: calc(34rpx * var(--font-scale));
  font-weight: bold;
  color: #333;
}

.modal-close {
  font-size: calc(50rpx * var(--font-scale));
  color: #999;
  line-height: 1;
  padding: 0 10rpx;
}

.modal-body {
  padding: 35rpx;
  max-height: 60vh;
  overflow-y: auto;
}

.modal-footer {
  display: flex;
  justify-content: flex-end;
  gap: 20rpx;
  padding: 25rpx 35rpx;
  border-top: 1rpx solid #e5e5e5;
}

.modal-footer .btn {
  min-width: 180rpx;
  height: 80rpx;
  line-height: 80rpx;
  border-radius: 10rpx;
  font-size: calc(28rpx * var(--font-scale));
  border: none;
}

.modal-footer .btn-cancel {
  background: #f5f5f5;
  color: #666;
}

.modal-footer .btn-primary {
  background: linear-gradient(135deg, var(--theme-primary) 0%, var(--theme-primary) 100%);
  color: #fff;
}

/* 预览弹窗内容 */
.preview-content {
  display: flex;
  flex-direction: column;
  gap: 25rpx;
}

.preview-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 20rpx 0;
  border-bottom: 1rpx solid #f0f0f0;
}

.preview-item:last-child {
  border-bottom: none;
}

.preview-label {
  font-size: calc(28rpx * var(--font-scale));
  color: #666;
}

.preview-value,
.preview-picker {
  font-size: calc(28rpx * var(--font-scale));
  color: #333;
  font-weight: 500;
}

.preview-input {
  width: 150rpx;
  height: 70rpx;
  line-height: 70rpx;
  border: 1rpx solid #ddd;
  border-radius: 8rpx;
  text-align: center;
  font-size: calc(28rpx * var(--font-scale));
}

.preview-picker {
  background: #f5f5f5;
  padding: 15rpx 20rpx;
  border-radius: 8rpx;
  display: flex;
  align-items: center;
}

.preview-picker::after {
  content: '›';
  margin-left: 10rpx;
  color: #999;
}

/* 加载状态 */
.loading-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 100vh;
  gap: 20rpx;
}

.loading-icon {
  font-size: calc(80rpx * var(--font-scale));
  animation: spin 1s linear infinite;
}

.loading-text {
  font-size: calc(28rpx * var(--font-scale));
  color: #999;
}

@keyframes spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

/* 响应式调整 */
@media screen and (max-width: 375px) {
  .shipping-page {
    padding: 30rpx;
  }
  
  .info-grid {
    grid-template-columns: 1fr;
  }
  
  .signature-section {
    flex-direction: column;
    gap: 20rpx;
  }
}
